{% extends 'base.html.twig' %}

{% block title %}Editer le tag {{ tag.name }}{% endblock %}

{% block body %}
    <h1>Editer le tag {{ tag.name }}</h1>

    <div class="box-body">
        {{ include('partials/_form.html.twig', {button_label: 'Éditer'}) }}
    </div>
{% endblock %}
