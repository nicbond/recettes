{% extends 'admin/base.html.twig' %}

{% block admin_title %}Editer la catégorie {{ category.name }}{% endblock %}

{% block admin_body %}
    <h1>Editer la catégorie {{ category.name }}</h1>

    <div class="box-body">
        {{ include('partials/_form.html.twig', {button_label: 'Editer'}) }}
    </div>
{% endblock %}
