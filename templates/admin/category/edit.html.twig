{% extends 'admin/admin.html.twig' %}

{% block title %}Editer la catégorie {{ category.name }}{% endblock %}

{% block body %}
    <h1>Editer la catégorie {{ category.name }}</h1>

    <div class="box-body">
        {{ include('partials/_form.html.twig', {'button_label': 'Editer'}) }}
    </div>
{% endblock %}
