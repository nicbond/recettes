{% extends 'admin/base.html.twig' %}

{% block admin_title %}Recette : {{ recipe.title }}{% endblock %}

{% block admin_body %}
    <h1>{{ recipe.title }}</h1>

    <p>
        {{ recipe.content|nl2br }}
    </p>

{% endblock %}
