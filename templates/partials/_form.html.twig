{{ form_start(form) }}
    {% for child in form %}
        {{ form_row(child) }}
    {% endfor %}
    {% if not show %}
        <button class="btn btn-primary float-right">{{ button_label|default('Enregistrer') }}</button>
    {% endif %}
{{ form_end(form) }}
