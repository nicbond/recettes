{% extends 'base.html.twig' %}

{% block title %}Recette : {{ recipe.title }}{% endblock %}

{% block body %}
    <h1>{{ recipe.title }}</h1>
    <div class="box-body">
        {{ include('recipe/_form.html.twig', {'button_label': 'Editer'}) }}
    </div>
{% endblock %}
